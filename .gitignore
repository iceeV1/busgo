# ข้อมูล runtime (ฐานข้อมูลการจอง) — server.js จะสร้างให้เองตอนรันครั้งแรก
data/

# dependencies (ถ้าต่อยอดใช้ npm ในอนาคต)
node_modules/
package-lock.json

# ไฟล์ระบบ / editor
.DS_Store
Thumbs.db
desktop.ini
*.log
.vscode/
.idea/

# ไฟล์ภาพ/เอกสารส่วนตัวที่โยนลงโฟลเดอร์ (ไม่ใช่โค้ด)
test.jpeg
*.ps1
